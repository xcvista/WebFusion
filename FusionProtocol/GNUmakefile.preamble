
# To compile this project, multiple clang flags are required.

CC := clang # Only clang builds it.

ADDITIONAL_OBJCFLAGS := -Wall -ObjC -fobjc-arc -Xclang -fobjc-default-synthesize-properties
ADDITIONAL_CFLAGS := -finline-functions -fblocks
